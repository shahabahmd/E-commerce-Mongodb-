<section class="success-section text-center">
    <div class="container mt-5">
        <h2 class="text-success">🎉 Order Placed Successfully!</h2>
        <p> <strong>{{user.name}}</strong></p>
        <h3 class="mt-3">Thank you, 
        <span class="text-primary">
            {{#if user}}
                {{user.Name}}
            {{else}}
                Guest
            {{/if}}
        </span> 
        for your order.
    </h3>
        <a href="/products" class="btn btn-primary mt-4">Continue Shopping</a>
    </div>
</section>


<!-- Custom CSS -->
<style>
    /* 🔹 Background */
    body {
        background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460);
        font-family: 'Poppins', sans-serif;
        color: white;
    }

    /* 🔹 Order Success Section */
    .order-success {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80vh;
        text-align: center;
    }

    /* 🔹 Premium Typography */
    h1 {
        font-size: 32px;
        font-weight: bold;
        color: #00d4ff;
    }

    /* 🔹 Custom Glassmorphism Button */
    .btn-glow {
        background: linear-gradient(45deg, #00d4ff, #007bff);
        font-size: 18px;
        border-radius: 8px;
        transition: all 0.3s ease-in-out;
        color: white;
        font-weight: bold;
        padding: 12px 24px;
        border: none;
        text-decoration: none;
        box-shadow: 0px 0px 15px rgba(0, 212, 255, 0.3);
    }

    .btn-glow:hover {
        background: linear-gradient(45deg, #00a3cc, #0056b3);
        box-shadow: 0px 0px 15px rgba(0, 212, 255, 0.6);
    }

    /* 🔹 Responsive Design */
    @media (max-width: 768px) {
        h1 {
            font-size: 26px;
        }

        .btn-glow {
            font-size: 16px;
            padding: 10px 20px;
        }
    }
</style>
