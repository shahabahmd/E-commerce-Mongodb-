<section class="product-section">
    <div class="container">
        <div class="row mt-4">
            <div class="col text-end">
                <a href="/admin/add-product" class="btn btn-glow shadow-lg px-4 py-2">➕ Add Product</a>
            </div>
        </div>

        <div class="table-responsive mt-4">
            <table class="table table-bordered table-hover table-striped align-middle shadow-sm" id="productsTable">
                <thead>
                    <tr class="text-center">
                        <th scope="col">Title</th>
                        <th scope="col">Description</th>
                        <th scope="col">Price</th>
                        <th scope="col">Image</th>
                        <th scope="col">Options</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                    <tr>
                        <td class="table-text">{{this.Name}}</td>
                        <td class="table-text">{{this.Description}}</td>
                        <td class="text-success fw-bold">₹{{this.Price}}</td>
                        <td class="text-center">
                            <img class="rounded shadow-sm" style="width: 60px; height: 60px; object-fit: cover;" src="/product-images/{{this._id}}.jpg" alt="Product Image">
                        </td>
                        <td class="text-center">
                            <a href="/admin/edit-product/{{this._id}}" class="btn btn-primary btn-sm px-3 me-2"> Edit</a>
                            <a href="/admin/delete-product/{{this._id}}" class="btn btn-danger btn-sm px-3" onclick="return confirm('Are you sure you want to delete {{this.Name}}?')"> Delete</a>
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
    </div>
</section>

<!-- DataTable Script -->
<script>
    $(document).ready(function () {
        $('#productsTable').DataTable({
            "paging": true,
            "ordering": true,
            "info": true,
            "autoWidth": false,
            "responsive": true,
            "columnDefs": [
                { "orderable": false, "targets": [3, 4] }
            ]
        });
    });
</script>

<!-- Custom CSS -->
<style>
    /* 🔹 Page Background */
    body {
        background: linear-gradient(135deg, #1a1a2e, #16213e, #0f3460);
        font-family: 'Poppins', sans-serif;
        color: white;
    }

    /* 🔹 Section Styling */
    .product-section {
        background: rgba(255, 255, 255, 0.1);
        padding: 50px;
        border-radius: 12px;
        box-shadow: 0px 4px 15px rgba(0, 0, 0, 0.2);
        backdrop-filter: blur(10px);
        margin-top: 50px;
    }

    /* 🔹 Table Header */
    thead {
        background: rgba(255, 255, 255, 0.2);
        color: white;
        text-transform: uppercase;
    }

    /* 🔹 Table Text */
    .table-text {
        color: white;
        word-wrap: break-word;
        max-width: 300px;
    }

    /* 🔹 Table Row Hover */
    .table-hover tbody tr:hover {
        background: rgba(255, 255, 255, 0.1);
    }
    
    /* 🔹 Buttons */
    .btn-glow {
        background: linear-gradient(45deg, #00d4ff, #007bff);
        font-size: 18px;
        border-radius: 8px;
        transition: all 0.3s ease-in-out;
        color: white;
        font-weight: bold;
        padding: 12px;
        border: none;
    }

    .btn-glow:hover {
        background: linear-gradient(45deg, #00a3cc, #0056b3);
        box-shadow: 0px 0px 15px rgba(0, 212, 255, 0.6);
    }

    .btn-primary, .btn-danger {
        border-radius: 6px;
        transition: 0.3s ease-in-out;
    }

    .btn-primary:hover {
        background: #0056b3;
        box-shadow: 0px 0px 8px rgba(0, 123, 255, 0.4);
    }

    .btn-danger:hover {
        background: #c82333;
        box-shadow: 0px 0px 8px rgba(220, 53, 69, 0.4);
    }
</style>
